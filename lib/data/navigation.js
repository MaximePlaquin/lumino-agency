/**
 * Récupère les éléments de navigation
 * Dans une implémentation réelle, cela serait remplacé par un appel à une API
 * @returns {Array} Les éléments de navigation
 */
export function getNavigationItems() {
  return [
    { id: 1, label: "Nos services", href: "#services" },
    { id: 2, label: "Qui sommes-nous", href: "#about" },
    { id: 3, label: "Pourquoi nous", href: "#why-us" },
    // { id: 4, label: "Portfolio", href: "#portfolio" },
    { id: 5, label: "Contact", href: "#contact" },
  ]
}
